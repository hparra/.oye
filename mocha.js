
const FS = require('fs')
const Assert = require('assert') // https://nodejs.org/api/assert.html

describe('Test Famlily', () => {
  it('should test', (done) => {
    // remember you can return Promises
    Assert.equal(false, true) // (actual, expected[, message])
  })
})
